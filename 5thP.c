#include <stdio.h> //BIt wise operstions
int main()
{
    int a,b;
    printf("Enter the value of a :");
    scanf("%d",&a);
    b = a << 1;
    printf("The left shift of c is: %d", b);
    return 0;
}